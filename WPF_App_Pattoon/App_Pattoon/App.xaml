<Application x:Class="Wpf_App_Pattoon_Animalerie.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Wpf_App_Pattoon_Animalerie"
             xmlns:vm="clr-namespace:Wpf_App_Pattoon_Animalerie.ViewModel.UserControlViewModel"
             xmlns:views="clr-namespace:Wpf_App_Pattoon_Animalerie.View.UserControlView"
             
             StartupUri="MainWindow.xaml">
    <Application.Resources>


        <Style x:Key="AnimatedContentControl" TargetType="ContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Grid>
                            <ContentPresenter x:Name="contentPresenter"
                                      Content="{TemplateBinding Content}"
                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                      Opacity="0"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="ContentPresenter.Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="contentPresenter"
                                                 Storyboard.TargetProperty="Opacity"
                                                 From="0" To="1" Duration="0:0:0.4"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Application Reel Resources-->

        <DataTemplate DataType="{x:Type vm:DemandeViewModel}">
            <views:DemandeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AnimalViewModel}">
            <views:AnimalView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ContactViewModel}">
            <views:ContactView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:EntreeViewModel}">
            <views:EntreeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SortieViewModel}">
            <views:SortieView/>
        </DataTemplate>

        <!-- Application Reel Options-->

        <DataTemplate DataType="{x:Type vm:VaccinViewModel}">
            <views:VaccinView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:Page1ViewModel}">
            <views:Page1View/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:Page2ViewModel}">
            <views:Page2View/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CompatibiliteViewModele}">
            <views:CompatibiliteView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CouleurViewModel}">
            <views:CouleurView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:TypeContactViewModel}">
            <views:TypeContactView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:MotifEntreeViewModel}">
            <views:MotifEntreeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:MotifSortieViewModel}">
            <views:MotifSortieView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AdoptionViewModel}">
            <views:AdoptionView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AccueilViewModel}">
            <views:AccueilView/>
        </DataTemplate>

    </Application.Resources>
</Application>
